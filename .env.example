# Environment Variables for Relax App

# Giphy API Key (Get from https://developers.giphy.com/)
# Replace 'your_actual_giphy_api_key_here' with your real API key
# Optional - app will use fallback GIFs if not provided and
# the current key is a public beta key is not suitable for production
# replace with your own key for dev / production use
GIPHY_API_KEY=dc6zaTOxFJmzC

# Flask Environment (development/production)
FLASK_ENV=development

# Port (Render sets this automatically in production)
# PORT=5000
